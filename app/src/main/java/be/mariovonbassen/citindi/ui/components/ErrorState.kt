package be.mariovonbassen.citindi.ui.components

import androidx.annotation.StringRes

/**
 * Error state holding values for error ui
 */

enum class ErrorType {
    LOGIN_ERROR,
    REGISTRATION_ERROR,
    ADDCITY_ERROR,
    CHANGE_ACCOUNT_ERROR
}